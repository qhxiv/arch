#!/bin/sh

if [[ -e $HOME/.lightmode ]]; then
  xfconf-query -c xsettings -p /Net/ThemeName -s "Adwaita-dark"
  rm -rf $HOME/.lightmode
else
  xfconf-query -c xsettings -p /Net/ThemeName -s "Adwaita"
  touch $HOME/.lightmode
fi

pkill -HUP -x dwm &
